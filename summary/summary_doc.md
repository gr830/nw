# JavaScript

**JavaScript (JS)** — это легковесный интерпретируемый (или JIT-компилируемый) язык программирования.JavaScript это прототипно-ориентированный, мультипарадигменный язык с динамической типизацией, который поддерживает объектно-ориентированный, императивный и декларативный (например, функциональное программирование) стили программирования. 

JavaScript — язык программирования, который позволяет создавать динамически обновляемый контент, управлять мультимедиа, анимировать изображения и делать многое другое.

API-интерфейсы браузера встроены в ваш веб-браузер и могут отображать данные из окружающего компьютерного окружения
- API-интерфейс DOM (Document Object Model) позволяет вам манипулировать HTML и CSS, создавать, удалять и изменять HTML, динамически применять новые стили к вашей странице и т. д..
- API геолокации извлекает географическую информацию
- API Canvas и WebGL позволяют создавать анимированные 2D и 3D-графику
- Аудио и видео API

**Безопасность браузера**
Каждая вкладка браузера представляет собой отдельную коробку для запуска кода (в техническом языке, эти коробки называются "средами исполнения") — это значит, что в большинстве случаев код на каждой вкладке запускается полностью отдельно, а код одной вкладки не может напрямую влиять на код другой вкладки или на другом веб-сайт.

**Последовательность выполнения JavaScript**
JavaScript, он запускает его по порядку, сверху вниз. Это значит, что вам нужно осторожно выбирать поряд

**Интерпретируемый против компилируемого кода**
JavaScript является интерпретируемым языком — код запускается сверху вниз и результат запуска немедленно возвращается. Вам не нужно преобразовывать код в другую форму, перед запуском в браузере.

**Серверный против клиентского кода**
Клиентский код — это код, который запускается на компьютере пользователя.

**Внутренний JavaScript**
```jsx
<script>
  // здесь будет JavaScript
</script>
```

**Внешний JavaScript**

JavaScript в отдельный файл
```jsx
<script src="script.js" defer></script>
```

**Стратегии загрузки скриптов**
Причиной, по которой мы поставили элемент script> в нижней части HTML файла, является то, что HTML-элементы загружаются браузером в том порядке, в котором они расположены в файле. Поэтому, если JavaScript загружается первым и ему нужно взаимодействовать с HTML ниже его, он не сможет работать, так как JavaScript будет загружен раньше, чем HTML, с которым нужно работать. Поэтому, располагать JavaScript в нижней части HTML страницы считается лучшей стратегией.

**Комментарии**
```jsx
// Я комментарий

/*
  Я тоже
  комментарий
*/
```

**Инлайновые JavaScript обработчики**

Вам нужно будет добавить атрибут onclick="createParagraph()" к каждой кнопке, к которой вы хотите подключить JavaScript.

```jsx
//<button onclick="createParagraph()">Click me!</button>
function createParagraph() {
  var para = document.createElement("p");
  para.textContent = "You clicked the button!";
  document.body.appendChild(para);
}
```

Использование чистой JavaScript конструкции, позволит вам выбрать все кнопки, используя одну команду
```jsx
const buttons = document.querySelectorAll("button");

for (let i = 0; i < buttons.length; i++) {
  buttons[i].addEventListener("click", createParagraph);
}
```

## Первое погружение в JavaScript

Чем больше вы будете заняты решением практических задач, тем быстрее будете расти в программировании

**Добавление переменных для хранения данных**
```jsx
var guessCount = 1;
var resetButton;
```

**Функции (Functions)**
Функции представляют собой многократно используемые блоки кода, написав один раз вы можете запускать их снова и снова
```jsx
function checkGuess() {
  alert("I am a placeholder");
}
```

**Операторы (Operators)**
Операторы JavaScript позволяют нам проводить проверки, математические расчёты, объединять строки вместе и выполнять другие подобные действия.
```jsx
var name = "Bingo";
name;
var hello = " says hello!";
hello;
var greeting = name + hello;
greeting;

//Также есть сокращённые операторы
name += " says hello!";
```

**Условные выражения (Conditionals)**
```jsx
if (guessCount === 1) {
    guesses.textContent = "Previous guesses: ";
}
```

**События (Events)**
События - это действия, которые происходят в браузере
```jsx
guessSubmit.addEventListener("click", checkGuess);
```

**Циклы (Loops)**
```jsx
for (var i = 1; i < 21; i++) {
  console.log(i);
}
```

## Устранение ошибок JavaScript

- Синтаксические ошибки: Это орфографические ошибки в коде, которые фактически заставляют программу вообще не запускаться, или перестать работать на полпути 
- Логические ошибки: Это ошибки, когда синтаксис действительно правильный, но код не тот, каким вы его предполагали, что означает, что программа работает успешно, но даёт неверные результаты

 console.log() - это часто используемая функция отладки, которая выводит значение в консоль.

## Переменные - место хранения необходимой информации

Переменные — это контейнер для таких значений, как числа, используемые в сложении, или строка, которую мы могли бы использовать как часть предложения. Но одна из особенностей переменных — их значение может меняться. Переменные не являются самими значениями; они представляют собой контейнеры для значений.

**Объявление переменной**
```jsx
var myName;//undefined
var myAge;////undefined
```

**Присвоение значения переменной**
```jsx
myName = "Chris";
myAge = 37;

//Вы можете объявить переменную и задать ей значение одновременно
var myName = "Chris";
```

**Обновление переменной**
```jsx
myName = "Bob";
myAge = 40;
```

**Правила именования переменных**
- Общепринято придерживаться так называемый "lower camel case
- Не используйте числа в начале переменных. Это недопустимо и приведёт к ошибке.
- не использовать "- _"

**Типы переменных**
- Числа (Numbers)
```jsx
var myAge = 17;
```
- Строки ('Strings')
```jsx
var dolphinGoodbye = "So long and thanks for all the fish";
```
- Логические (Booleans)
```jsx
var iAmAlive = true;
```
- Массивы (Arrays)
```jsx
var myNameArray = ["Chris", "Bob", "Jim"];
var myNumberArray = [10, 15, 40];
```
- Объекты (Objects)
```jsx
var dog = { name: "Spot", breed: "Dalmatian" };
```

**Динамическая типизация**
```jsx
var myNumber = "500"; // упс, это все ещё строка (string)
typeof myNumber;
myNumber = 500; // так-то лучше, теперь это число (number)
typeof myNumber;
```

## Базовая математика в JavaScript — числа и операторы

**Типы чисел**
- Целые — это целые числа, такие как: 10, 400, или -5.
- С плавающей точкой (или: с плавающей запятой) — имеют целую и дробную части, например: 12.5 или 56.7786543.
- Doubles — тип чисел с плавающей точкой, которые имеют большую точность, чем стандартные числа с плавающей точкой (что означает, что они точны для большего числа десятичных знаков).

**Разные типы числовых систем:**
- Бинарная — низкоуровневый язык компьютеров; нули и единицы (0 и 1);
- Восьмеричная — 8-ми разрядная, использует 0–7 в каждом столбце;
- Десятичная — 10-ти разрядная, использует 0-9 в каждом столбце;
- Шестнадцатеричная — 16-ти разрядная, используют 0–9 и потом a–f в каждом столбце. Вы, возможно, уже встречали эти числа, когда задавали цвет в CSS.

```jsx
var myInt = 5;
var myFloat = 6.667;
myInt;
myFloat;
```

**Арифметические операторы**
" + - / * ** %-11 % 3 = 2 (поскольку число 3 вмещается три раза, остатком будет числа)"

```jsx
10 + 7;
9 * 8;
60 % 3;

var num1 = 10;
var num2 = 50;
9 * num1;
num2 / num1;

5 + 10 * 3;
(num2 % 9) * num1;
num2 + num1 / 8 + 2;
```

**Приоритет операторов**
Приоритет операторов - некоторые операторы будут применены перед другими в процесс вычисле
```jsx
(num2 + num1) / (8 + 2);
```

**Операторы инкремента и декремента**
```jsx
//Таким образом, вы можете применить инкремент только к существующим переменным 
var num1 = 4;
num1++;
```

Вы можете заставить делать это в другом порядке - применить инкремент/декремент и только потом вернуть значение. Для этого необходимо записать оператор слева от переменной, а не справо.

**Операторы присваивания**
```jsx
var x = 3; // x содержит значение 3
var y = 4; // y содержит значение 4
x *= y; // x содержит значение 12
```

**Операторы сравнения**
"== === !== > <="

## Работа с текстом — строки в JavaScript

```jsx
//Одиночные кавычки vs. Двойные кавычки
const sgl = "Одиночные кавычки.";
const dbl = "Двойные кавычки.";

//Экранирование кавычек в строках -  они распознаются как текст, а не часть кода
const bigmouth = "Жанна Д'Арк — народная героиня Франции.";
bigmouth;

//Конкатенация строк - слово, которое означает объединить
const one = "Привет, ";
const two = "как дела?";
const joined = one + two;

const response = one + "Я в порядке — " + two;
response;

//заставить число стать строкой, обернув её в кавычки
const myDate = "19" + "67";

//преобразовать в строчную
const myString = "123";
const myNum = Number(myString)
const myNum = 123;
const myString = myNum.toString();//метод

//Совмещение строк с различными выражениями
const song = "Fight the Youth";
const score = 9;
const highestScore = 10;
const output = `Мне нравится песня ${song}. Я оценил её на ${
  (score / highestScore) * 100
}%.`;
console.log(output); // "Мне нравится песня Fight the Youth. Я оценил её на 90%."

//Многострочный текст
const output = "Мне нравится эта песня.\nЯ оценил её на 90%"
//or
const output = `Мне нравится эта песня. 
  Я оценил её на 90%.`;
console.log(output);
/*
  Мне нравится эта песня.
  Я оценил её на 90%.
  */
```

## Полезные методы для строк

Почти всё в JavaScript является объектами,ваша переменная становится строковым объектом, и, как результат, ей доступно множество свойств и методов.

```jsx
//Поиск длины строки
let browserType = 'mozilla';
browserType.length;//7

//Получение определённого строкового символа
browserType[0];
//Длина слова «mozilla» равна 7, но, поскольку счёт начинается с 0, позиция последнего символа равна 6, поэтому нам нужна length
browserType[browserType.length-1]

//Поиск подстроки внутри строки и её извлечение
browserType.indexOf('zilla');//2
browserType.indexOf('vanilla');// -1, не найдена

if(browserType.indexOf('mozilla') === -1) {
  // сделать что-то, если 'mozilla'
  // не является частью этой строки
}

//извлечения
browserType.slice(0,3);//moz
browserType.slice(2);//zilla - начать извлечение оставшихся символов в строке

//Изменение регистра
var radData = 'My NaMe Is MuD';
radData.toLowerCase();
radData.toUpperCase();

//Обновление частей строки
browserType.replace('moz','van');

```

## Массивы

Массивы обычно описываются как «объекты, подобные спискам

```jsx
//Создание массива
var shopping = ["bread", "milk", "cheese", "hummus", "noodles"];
var sequence = [1, 1, 2, 3, 5, 8, 13];
var random = ["tree", 795, [0, 1, 2]];

//Получение и изменение элементов массива
shopping[0];// возвращает "bread"

//изменять элемент в массиве
shopping[0] = "tahini"; 

//Заметьте, что массив внутри массива называется многомерным массивом
random[2][2];

//Нахождение длины массива
sequence.length;

//Преобразование между строками и массивам
var myData = "Manchester,London,Liverpool,Birmingham,Leeds,Carlisle";
var myArray = myData.split(",") //[]
var myNewString = myArray.join(",");//jобратно

var dogNames = ["Rocket", "Flash", "Bella", "Slugger"];
dogNames.toString(); //Rocket,Flash,Bella,Slugger

//Добавление и удаление элементов массива
var myArray = [
  "Manchester",
  "London",
  "Liverpool",
  "Birmingham",
  "Leeds",
  "Carlisle",
];

//При завершении вызова метода возвращается новая длина массива
myArray.push("Cardiff");
myArray.push("Bradford", "Brighton");
//Удаление последнего элемента,возвращается удалённый элемент
myArray.pop();

//unshift() и shift() работают точно таким же способом, за исключением того что они работают в начале массива

```
## Условные конструкции

```jsx
if (condition) {
  code to run if condition is true
} else {
  run some other code instead
}

//Это абсолютно рабочий код, но он менее читаем, лучше использовать фигурные скобки, новые строки и отступы
if (condition) code to run if condition is true
else run some other code instead

var shoppingDone = false;

if (shoppingDone === true) {
  var childsAllowance = 10;
} else {
  var childsAllowance = 5;
}
```

**Примечание об операторах сравнени**

- === и !== — проверяет одно значение идентично или не идентично другому.
- < и > — проверяет одно значение меньше или больше, чем другое.
- <= и >= — проверяет одно значение меньше или равно, либо больше или равно другому.

Любое значение, которое не есть false, undefined, null, 0, NaN, или пустая строка ('') фактически возвращает true

Вполне нормально использовать один условный оператор if...else внутри другого — вложить их. 
```jsx
if (choice === "sunny") {
  if (temperature < 86) {
    para.textContent =
      "Сейчас " +
      temperature +
      " градусов по фаренгейту — хорошо и солнечно. Идите на пляж, или в парк, и купите мороженое.";
  } else if (temperature >= 86) {
    para.textContent =
      "Сейчас " +
      temperature +
      " градусов по фаренгейту — Жара! Если вы хотите выйти на улицу, обязательно используйте солнцезащитный крем.";
  }
}
```

**Логические операторы: И, ИЛИ и НЕ**

- && — И; позволяет объединить два или более выражения так, что каждое из них отдельно должно иметь значение true , чтобы в итоге общее выражение имело значение true.
- || — ИЛИ; позволяет объединить два или более выражения так, что одно или несколько из них должно иметь значение true , чтобы в итоге общее выражение имело значение true
- Последний тип логического оператора НЕ, выраженный ! оператором, можно использовать для отрицания выражения.

```jsx
if (!(iceCreamVanOutside || houseStatus === "on fire")) {
  console.log("Вероятно, можно в нем оставаться.");
} else {
  console.log("Вы должны быстро покинуть дом.");
}
```

Ошибки - не верно
```jsx
//не верно
if (x === 5 || 7 || 10 || 20) {
  // выполнить код
}

//верно
if (x === 5 || x === 7 || x === 10 || x === 20) {
  // выполнить код
}
```

**Оператор switch**

Оператор switch – он принимает одно единственное выражение или значение, а затем просматривает ряд вариантов, пока не найдут вариант, соответствующий этому значению

```jsx
switch (выражение) {
  case choice1:
    выполнить этот код
    break;

  case choice2:
    выполнить этот код, а не предыдущий
    break;

  // вариантов может быть любое количество

  default:
    а вообще-то, выполнить только этот код
}
```

**Тернарный оператор**

```jsx
( условие) ? выполнить этот код : выполнить этот код вместо первого

var greeting = isBirthday
  ? "С днём рождения, г-н Кузнецов! Хорошо вам повеселиться!"
  : "Доброе утро, г-н Кузнецов.";
```








